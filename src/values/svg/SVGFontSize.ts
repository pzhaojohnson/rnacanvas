import { PositiveFiniteNumber } from 'Values/PositiveFiniteNumber';

export {
  /**
   * Currently the SVGFontSize class is just an alias for the
   * PositiveFiniteNumber class.
   */
  PositiveFiniteNumber as SVGFontSize,
};
